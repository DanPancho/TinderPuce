const publicRoutes = {
    HOME: '/',
    LOGIN: '/login',
    REGISTER: '/Register/NewUser',
    CONFIGURATION: '/configuration',
    INIT: '/Home'
}

const Routes = {
    ...publicRoutes
}

export default Routes;